PASTE THIS INTO CLAUDE CODE
===========================

cd ~/Documents/GitHub/caissepro-v2

I'm redesigning the landing page to be visual-first. This folder
(design_handoff_landing/) contains a full design spec and assets.

TASK:
1. Read design_handoff_landing/README.md in full — it documents every
   section, color, font, measurement, and interaction.
2. Open design_handoff_landing/design-reference.html in your head as
   the visual target (it's an HTML prototype, NOT code to copy — rebuild
   it in our real stack: Next.js 14 + TypeScript + Tailwind).
3. Copy the 6 images from design_handoff_landing/public/ into our
   public/ folder.
4. Rebuild app/page.tsx to match the design. Split into components the
   way our codebase already does. Only the Pricing toggle (and scroll
   animations) need "use client".

MUST FOLLOW (our standards):
- Premium dark theme, emerald #16a34a primary, #22c55e bright accent
- Sora font for headings (font-black), Manrope for body (load via next/font)
- rounded-[2rem] cards, rounded-2xl buttons
- Lucide React icons ONLY, no emojis
- All UI text in French
- Fully responsive, mobile-first
- "Propulsé par AMDY LABS" in footer
- Smooth scroll reveal animations

After building:
  npm run dev          # I'll review at localhost:3000 first
Then once I approve:
  git add -A && git commit -m "Redesign landing page: visual-first with device mockups" && git push

Start by reading the README and confirming the section list back to me.
